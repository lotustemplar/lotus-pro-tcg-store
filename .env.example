# --- Database ---
# Local dev (SQLite, zero setup):
DATABASE_URL="file:./dev.db"
# Production (Postgres, e.g. Neon/Supabase/Railway):
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# --- Stripe ---
# Get these from https://dashboard.stripe.com/test/apikeys (use test keys until launch)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Admin login ---
# Single-admin gate for /admin. Change these before deploying.
ADMIN_EMAIL="admin@lotusprodecks.com"
ADMIN_PASSWORD="change-me-now"

# Signs the admin session cookie — set to a long random string in production
ADMIN_SESSION_SECRET="dev-only-insecure-secret-change-me"

# Optional: if set, saving Site Settings in /admin will also trigger a Netlify
# production rebuild. Without this, DB-backed content changes are still live on refresh.
NETLIFY_BUILD_HOOK_URL="https://api.netlify.com/build_hooks/your-hook-id"
